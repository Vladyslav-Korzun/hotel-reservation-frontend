export const environment = {
  production: true,
  apiBaseUrl: '/api',
  auth: {
    issuer: 'https://auth.example.com/realms/hotel-reservation',
    clientId: 'hotel-reservation-client',
    requireHttps: true,
  },
};
