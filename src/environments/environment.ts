export const environment = {
  production: true,
  apiBaseUrl: '/api',
  auth: {
    issuer: 'https://hotel-reservation-system.com/auth/realms/hotel-reservation',
    clientId: 'hotel-reservation-frontend',
    requireHttps: true,
  },
};
